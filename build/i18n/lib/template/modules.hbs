{{#modules}}
    {{&name}}: {
        {{#messages}}
            {{&key}}: '{{&value}}'{{#if @last}}{{else}},{{/if}}
        {{/messages}} 
    {{#if @last}}
    }
    {{else}}
    },
    {{/if}}
{{/modules}}
